-- Rename tables from English to Portuguese
RENAME TABLE `aircraft` TO `aeronaves`;
RENAME TABLE `parts` TO `pecas`;
RENAME TABLE `employees` TO `funcionarios`;
RENAME TABLE `stages` TO `etapas`;
RENAME TABLE `tests` TO `testes`;
RENAME TABLE `reports` TO `relatorios`;
RENAME TABLE `metrics` TO `metricas`;
